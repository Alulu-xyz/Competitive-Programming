#include <bits/stdc++.h>
using namespace std;


int main(){
// sorting a verctor using the sorting library
    vector<int> v = {4, 2, 5,3 5, 8, 3};
    sort(v.begin(), v.end());

    // normal arr
    int n = 7;
    int a[]= {4,2,5,3,5,8,3};
    sort(a,a+n);
}
